 (() => {   "use strict";      const BACKEND_BASE_URL = "https:   const CACHE_KEY = "lypo_blog_cache_v2";    const CACHE_DURATION = 10 * 60 * 1000;       const list = document.getElementById("blogList");   if (!list) return;        const escapeMap = {      "&": "&amp;",      "<": "&lt;",      ">": "&gt;",      '"': "&quot;",     "'": "&#x27;",     "/": "&#x2F;"   };   const esc = (s) => String(s || "").replace(/[&<>"'\/]/g, c => escapeMap[c]);       const sanitizeUrl = (url) => {     if (!url) return "";     const urlStr = String(url).trim().toLowerCase();          if (urlStr.startsWith("javascript:") ||          urlStr.startsWith("data:") ||          urlStr.startsWith("vbscript:")) {       return "";     }     return String(url).trim();   };       const fmtDate = (iso) => {     if (!iso) return "";     try {       const date = new Date(iso);       if (isNaN(date.getTime())) return "";       return date.toLocaleDateString(undefined, {          year: 'numeric',          month: 'short',          day: 'numeric'        });     } catch {       return "";     }   };       function getCached() {     try {       const cached = localStorage.getItem(CACHE_KEY);       if (!cached) return null;              const parsed = JSON.parse(cached);       if (!parsed || !parsed.data || !Array.isArray(parsed.data)) {                  localStorage.removeItem(CACHE_KEY);         return null;       }              const age = Date.now() - (parsed.timestamp || 0);              if (age < CACHE_DURATION) {         return parsed.data;       }                     localStorage.removeItem(CACHE_KEY);       return null;     } catch (e) {              try {         localStorage.removeItem(CACHE_KEY);       } catch {}       return null;     }   }       function setCache(data) {     if (!Array.isArray(data)) return;          try {              const validData = data.filter(p => p && typeof p === 'object');              localStorage.setItem(CACHE_KEY, JSON.stringify({         data: validData,         timestamp: Date.now(),         version: 2       }));     } catch (e) {              console.warn("Cache write failed, clearing old cache:", e);       try {                  Object.keys(localStorage).forEach(key => {           if (key.startsWith('lypo_blog_') || key.startsWith('lypo_post_')) {             localStorage.removeItem(key);           }         });       } catch {}     }   }       function renderPosts(posts) {     if (!posts || !Array.isArray(posts) || !posts.length) {       return '<div class="mutedCell" role="status">No posts yet.</div>';     }           const limitedPosts = posts.slice(0, 50);      return limitedPosts.map(p => {              if (!p || typeof p !== 'object' || !p.slug || !p.title) {         return '';       }               const safeSlug = encodeURIComponent(String(p.slug || '').trim());       const safeTitle = esc(p.title);       const safeExcerpt = esc(p.excerpt || "");       const safeDate = esc(fmtDate(p.published_at));       const safeCoverUrl = sanitizeUrl(p.cover_url);                     const cover = safeCoverUrl && safeCoverUrl.startsWith('https:         ? `<div class="blogCover"><img src="${esc(safeCoverUrl)}" alt="${safeTitle}" loading="lazy" decoding="async" crossorigin="anonymous" /></div>`          : "";              return `         <a class="blogItem" href="post.html?slug=${safeSlug}" rel="noopener" data-post="${safeSlug}">           ${cover}           <div class="blogMeta">             <div class="blogTitle">${safeTitle}</div>             <div class="blogSub">${safeExcerpt}</div>             ${safeDate ? `<div class="blogDate">${safeDate}</div>` : ''}           </div>         </a>       `;     }).filter(Boolean).join("");   }       async function load() {          list.innerHTML = '<div class="mutedCell" role="status" aria-live="polite">Loading posts...</div>';               const cached = getCached();     if (cached && cached.length > 0) {       list.innerHTML = renderPosts(cached);              fetchAndCache(true);       return;     }           await fetchAndCache(false);   }       async function fetchAndCache(silent = false) {     let timeoutId;     try {       const controller = new AbortController();              timeoutId = setTimeout(() => controller.abort(), 45000);        const res = await fetch(`${BACKEND_BASE_URL}/api/blog/posts`, {         signal: controller.signal,         method: 'GET',         headers: {           'Accept': 'application/json',           'X-Requested-With': 'XMLHttpRequest'          },         credentials: 'omit',          mode: 'cors',         cache: 'no-cache'        });              if (timeoutId) clearTimeout(timeoutId);        if (!res.ok) {         throw new Error(`HTTP ${res.status}`);       }               const contentType = res.headers.get('content-type');       if (!contentType || !contentType.includes('application/json')) {         throw new Error('Invalid response type');       }        const data = await res.json();                     if (!data || typeof data !== 'object') {         throw new Error('Invalid response format');       }              const posts = Array.isArray(data.posts) ? data.posts : [];                     if (posts.length > 0) {         setCache(posts);       }                     if (!silent) {         list.innerHTML = renderPosts(posts);       }            } catch (e) {       if (timeoutId) clearTimeout(timeoutId);       console.error("Blog load error:", e);                     if (silent) return;                     const errorMsg = e.name === 'AbortError'          ? '⏱ Server is waking up (free tier cold start). Please wait and retry in 10 seconds...'          : '⚠️ Could not load posts. Please try again.';              list.innerHTML = `         <div class="mutedCell" role="alert">           ${esc(errorMsg)}            <button onclick="location.reload()" style="margin-left:8px; padding:4px 12px; background:rgba(124,58,237,.9); border:1px solid rgba(124,58,237,1); border-radius:6px; color:#fff; cursor:pointer; font:inherit; font-weight:600;">             Retry Now           </button>         </div>       `;     }   }       if (document.readyState === 'loading') {     document.addEventListener("DOMContentLoaded", load, { once: true, passive: true });   } else {     load();   }         list.addEventListener('mouseover', (e) => {     const link = e.target.closest('a[data-post]');     if (link && link.href) {       const prefetchLink = document.createElement('link');       prefetchLink.rel = 'prefetch';       prefetchLink.href = link.href;       document.head.appendChild(prefetchLink);     }   }, { passive: true, capture: true }); })(); 
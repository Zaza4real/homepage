 function enterResetMode({ email, token }) {      const wrap = $("resetWrap"); if (wrap) wrap.style.display = "";   setResetMsg("");       const toggle = $("authToggle"); if (toggle) toggle.style.display = "none";    const confirmWrap = $("confirmPassWrap"); if (confirmWrap) confirmWrap.style.display = "none";    const loginPass = $("authPassPage");   const loginPassLabel = loginPass ? loginPass.closest("label") : null;   if (loginPassLabel) loginPassLabel.style.display = "none";       const btnLogin = $("btnLoginPage"); if (btnLogin) btnLogin.style.display = "none";   const btnSignup = $("btnSignupPage"); if (btnSignup) btnSignup.style.display = "none";       const emailInput = $("authEmailPage");   if (emailInput) {     emailInput.value = email || "";     emailInput.disabled = true;   }    const p1 = $("resetPass1"); const p2 = $("resetPass2");   p1 && (p1.value = "");   p2 && (p2.value = "");    const btn = $("btnResetPass");   if (btn) {     btn.onclick = async () => {       const pass1 = p1?.value || "";       const pass2 = p2?.value || "";       if (!pass1 || !pass2) return setResetMsg("Please enter your new password twice.");       if (pass1 !== pass2) return setResetMsg("Passwords do not match.");       if (pass1.length < 6) return setResetMsg("Password too short (min 6 characters).");        try {         btn.disabled = true;         setResetMsg("Updating password…");         await apiFetch("/api/auth/reset-password", {           method: "POST",           body: JSON.stringify({ email, token, password: pass1 }),         });         setResetMsg("✅ Password updated. You can now log in.");                  history.replaceState(null, "", "auth.html");         const wrap2 = $("resetWrap"); if (wrap2) wrap2.style.display = "none";         if (toggle) toggle.style.display = "";         if (loginPassLabel) loginPassLabel.style.display = "";         if (btnLogin) btnLogin.style.display = "";         if (btnSignup) btnSignup.style.display = "";         if (emailInput) emailInput.disabled = false;         $("authPassPage")?.focus();       } catch (e) {         setResetMsg(e?.message || "Could not reset password. Please request a new link.");       } finally {         btn.disabled = false;       }     };   } }  async function maybeHandlePasswordReset() {   const hash = String(location.hash || "");   if (!hash.includes("reset=")) return false;    const params = new URLSearchParams(hash.replace(/^#/, ""));   const token = params.get("reset") || "";   const email = params.get("email") || "";       enterResetMode({ email, token });      history.replaceState(null, "", "auth.html");   return true; }   function initAuthToggle(){   const tabLogin = $("tabLogin");   const tabSignup = $("tabSignup");   const confirm = $("confirmPassWrap");    if (!tabLogin || !tabSignup) return;    const setMode = (mode) => {     if (mode === "login") {       tabLogin.classList.add("isActive");       tabSignup.classList.remove("isActive");       if (confirm) confirm.style.display = "none";     } else {       tabSignup.classList.add("isActive");       tabLogin.classList.remove("isActive");       if (confirm) confirm.style.display = "none";     }     $("authMsgPage").textContent = "";   };    tabLogin.addEventListener("click", () => setMode("login"));   tabSignup.addEventListener("click", () => setMode("signup"));   setMode("login"); }  (() => {   const BACKEND_BASE_URL = (location.hostname === "localhost" || location.hostname === "127.0.0.1")     ? "http://localhost:10000"     : "https://lypo-backend.onrender.com";   const AUTH_TOKEN_KEY = "lypo_token_v1";         const GOOGLE_CLIENT_ID = "1015062802676-jgvv6l7mt6isq3154t39bkn29a4cqml7.apps.googleusercontent.com";         const DISABLE_GOOGLE_SIGNIN = false;     const $ = (id) => document.getElementById(id);       let msgTimeout = null;   function setMsg(msg) {     const el = $("authMsgPage");     if (!el) return;               if (msgTimeout) {       clearTimeout(msgTimeout);       msgTimeout = null;     }               if (!msg || msg.trim() === "") {       el.style.opacity = "0";       msgTimeout = setTimeout(() => {         el.textContent = "";         el.style.opacity = "0";       }, 150);       return;     }               el.textContent = msg;               void el.offsetHeight;               el.style.opacity = "1";   }    function showConfirmPass(show){     const wrap = $("confirmPassWrap");     if (wrap) wrap.style.display = show ? "" : "none";   }    async function apiFetch(path, opts = {}) {     const headers = new Headers(opts.headers || {});     if (!headers.has("Content-Type")) headers.set("Content-Type", "application/json");     const res = await fetch(`${BACKEND_BASE_URL}${path}`, { ...opts, headers });     let data = null;     const ct = res.headers.get("content-type") || "";     if (ct.includes("application/json")) data = await res.json().catch(() => null);     else data = await res.text().catch(() => null);     if (!res.ok) {       const msg = data?.error || (typeof data==="string" && data.trim()) || `Request failed (${res.status})`;       const e = new Error(msg);       e.status = res.status;       throw e;     }     return data;   }     async function ensureBackendUp() {     try {       const r = await fetch(`${BACKEND_BASE_URL}/health`);       if (!r.ok) throw new Error(`Backend unhealthy (${r.status})`);       return true;     } catch (e) {       setMsg(`Backend not reachable. Check Render backend URL and logs. (${e.message || e})`);       return false;     }   }    async function doSignup() {     if (!(await ensureBackendUp())) return;      const email = $("authEmailPage")?.value?.trim();     const password = $("authPassPage")?.value || "";     const password2 = $("authPass2Page")?.value || "";               setMsg("");               if (!email || !password) {        setMsg("❌ Please enter email and password.");        return;      }               const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;     if (!emailRegex.test(email)) {       setMsg("❌ Please enter a valid email address.");       return;     }               if (password.length < 6) {       setMsg("❌ Password must be at least 6 characters long.");       return;     }               if (password !== password2) {        setMsg("❌ Passwords do not match.");        return;      }               if (password.length < 8) {       setMsg("⚠️ Password is weak. Consider using 8+ characters for better security.");       await new Promise(resolve => setTimeout(resolve, 2000));     }          try {       setMsg("Creating account…");       const out = await apiFetch("/api/auth/signup", {         method: "POST",         body: JSON.stringify({ email, password })       });       localStorage.setItem(AUTH_TOKEN_KEY, out.token);                     if (typeof trackConversion === "function") {         trackConversion("signup_success", null);       }              setMsg("✅ Account created! Redirecting…");       setTimeout(() => {         window.location.href = "index.html";       }, 1000);     } catch (e) {              if (typeof trackEvent === "function") {         trackEvent("signup_failed", { error: e.message });       }       setMsg(`❌ ${e.message || "Could not create account. Please try again."}`);     }   }    async function doLogin() {     if (!(await ensureBackendUp())) return;      const email = $("authEmailPage")?.value?.trim();     const password = $("authPassPage")?.value || "";               setMsg("");               if (!email || !password) {        setMsg("❌ Please enter email and password.");        return;      }               const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;     if (!emailRegex.test(email)) {       setMsg("❌ Please enter a valid email address.");       return;     }          try {       setMsg("Logging in…");       const out = await apiFetch("/api/auth/login", {         method: "POST",         body: JSON.stringify({ email, password })       });       localStorage.setItem(AUTH_TOKEN_KEY, out.token);                     if (typeof trackEvent === "function") {         trackEvent("login_success", { method: "email" });       }              setMsg("✅ Logged in! Redirecting…");       setTimeout(() => {         window.location.href = "index.html";       }, 1000);     } catch (e) {              if (typeof trackEvent === "function") {         trackEvent("login_failed", { method: "email", error: e.message });       }       setMsg(`❌ ${e.message || "Invalid email or password."}`);     }   }    async function handleGoogleSignIn(credential) {     try {       setMsg("Signing in with Google…");       const out = await apiFetch("/api/auth/google", {         method: "POST",         body: JSON.stringify({ credential })       });       localStorage.setItem(AUTH_TOKEN_KEY, out.token);                     if (typeof trackConversion === "function") {         trackConversion("google_signin_success");       }              setMsg("✅ Signed in! Redirecting…");       setTimeout(() => {         window.location.href = "index.html";       }, 1000);     } catch (e) {              if (typeof trackEvent === "function") {         trackEvent("google_signin_failed", { error: e.message });       }       setMsg(`❌ ${e.message || "Google sign-in failed. Please try again."}`);     }   }       function initGoogleSignIn() {          if (DISABLE_GOOGLE_SIGNIN) {       console.log("🔒 Google Sign-In disabled");       const btn = document.getElementById("googleSignInBtn");       if (btn) btn.style.display = "none";                     const divider = btn?.previousElementSibling;       if (divider) divider.style.display = "none";       return;     }          if (typeof google === 'undefined' || !google.accounts) {       console.warn("Google Sign-In not loaded yet, retrying...");       setTimeout(initGoogleSignIn, 100);       return;     }      google.accounts.id.initialize({       client_id: GOOGLE_CLIENT_ID,       callback: (response) => {         handleGoogleSignIn(response.credential);       },       auto_select: false,       cancel_on_tap_outside: true     });      google.accounts.id.renderButton(       document.getElementById("googleSignInBtn"),       {         theme: "filled_black",         size: "large",         type: "standard",         text: "continue_with",         shape: "rectangular",         logo_alignment: "left",         width: 280       }     );   }     async function maybeHandleEmailVerify() {   const hash = String(location.hash || "");   if (!hash.includes("verify=")) return false;    const params = new URLSearchParams(hash.replace(/^#/, ""));   const token = params.get("verify") || "";   const email = params.get("email") || "";       history.replaceState(null, "", "auth.html");    try {     setMsg("Verifying email…");     await apiFetch("/api/auth/verify-email", {       method: "POST",       body: JSON.stringify({ email, token }),     });     setMsg("✅ Email verified! You can now log in.");          showConfirmPass(false);     $("authEmailPage") && ($("authEmailPage").value = email);     $("authPassPage")?.focus();     return true;   } catch (e) {     setMsg(e?.message || "Could not verify email.");     return true;   } }    function setResetMsg(msg){   const el = document.getElementById("resetMsg");   if (el) el.textContent = msg || ""; }  async function maybeHandlePasswordReset() {   const hash = String(location.hash || "");   if (!hash.includes("reset=")) return false;    const params = new URLSearchParams(hash.replace(/^#/, ""));   const token = params.get("reset") || "";   const email = params.get("email") || "";       const resetCard = document.getElementById("resetCard");   if (resetCard) resetCard.style.display = "block";       const authCard = document.querySelector(".authCard");   if (authCard) authCard.style.display = "none";       history.replaceState(null, "", "auth.html");    const btn = document.getElementById("btnDoReset");   if (!btn) return true;    btn.onclick = async () => {     const p1 = document.getElementById("resetPass1")?.value || "";     const p2 = document.getElementById("resetPass2")?.value || "";     if (!p1 || !p2) { setResetMsg("Please enter your new password twice."); return; }     if (p1 !== p2) { setResetMsg("Passwords do not match."); return; }     if (p1.length < 6) { setResetMsg("Password too short (min 6 characters)."); return; }      try {       btn.disabled = true;       setResetMsg("Updating password…");       await apiFetch("/api/auth/reset-password", {         method: "POST",         body: JSON.stringify({ email, token, password: p1 }),       });       setResetMsg("✅ Password updated. You can now log in.");              if (authCard) authCard.style.display = "";       if (resetCard) resetCard.style.display = "none";       document.getElementById("authEmailPage") && (document.getElementById("authEmailPage").value = email);       document.getElementById("authPassPage")?.focus();     } catch (e) {       setResetMsg(e?.message || "Could not reset password. Please request a new link.");     } finally {       btn.disabled = false;     }   };    return true; }    function redirectToResetPageIfNeeded(){   const hash = String(location.hash || "");   if (!hash.includes("reset=")) return false;   const params = new URLSearchParams(hash.replace(/^#/, ""));   const token = params.get("reset") || "";   const email = params.get("email") || "";   if (!token || !email) return false;      location.replace(`reset.html?token=${encodeURIComponent(token)}&email=${encodeURIComponent(email)}`);   return true; }  document.addEventListener("DOMContentLoaded", () => {          initGoogleSignIn();               const passInput = $("authPassPage");          if (passInput) {              let validationTimeout = null;       passInput.addEventListener("input", () => {         const value = passInput.value;                           if (validationTimeout) {           clearTimeout(validationTimeout);         }                           validationTimeout = setTimeout(() => {           if (value.length > 0 && value.length < 6) {             setMsg("⚠️ Password must be at least 6 characters.");           } else if (value.length === 0) {             setMsg("");           } else {                          const currentMsg = $("authMsgPage")?.textContent || "";             if (currentMsg.includes("⚠️ Password")) {               setMsg("");             }           }         }, 300);        }, { passive: true });     }          $("btnSignupPage")?.addEventListener("click", () => {     const wrap = $("confirmPassWrap");     if (wrap && wrap.style.display === "none") {       wrap.style.display = "";       $("authPass2Page")?.focus();       setMsg("Please confirm your password to continue.");       return;     }     doSignup().catch(e => setMsg(`❌ ${e.message || "Sign up failed."}`));   });     $("btnLoginPage")?.addEventListener("click", () => doLogin().catch(e => setMsg(`❌ ${e.message || "Login failed."}`)));           ["authEmailPage","authPassPage"].forEach((id) => {       $(id)?.addEventListener("keydown", (e) => {         if (e.key === "Enter") $("btnLoginPage")?.click();       });     });   });       function getHashParams() {     const h = (location.hash || "").replace(/^#/, "");     const out = {};     if (!h) return out;     for (const part of h.split("&")) {       const [k, v] = part.split("=");       if (k) out[decodeURIComponent(k)] = decodeURIComponent(v || "");     }     return out;   }    async function requestPasswordReset() {     const email = $("authEmailPage")?.value?.trim();     if (!email) return setMsg("Please enter your email first.");     try {       const forgotCandidates = ["/api/auth/forgot-password", "/api/auth/forgot", "/api/auth/request-reset", "/api/auth/password/forgot", "/auth/forgot-password", "/auth/forgot", "/auth/request-reset", "/auth/password/forgot", "/forgot-password", "/forgot"];       let ok = false;       let lastErr = null;       for (const url of forgotCandidates) {         try {           await apiFetch(url, { method: "POST", body: JSON.stringify({ email }) });           ok = true;           break;         } catch (e) {           lastErr = e;         }       }       if (!ok) throw lastErr; setMsg("If an account exists for that email, a reset link has been sent.");     } catch (e) {              setMsg("If an account exists for that email, a reset link has been sent.");     }   }    async function doResetPassword() {     const params = getHashParams();     const token = params.reset || "";     const email = (params.email || $("authEmailPage")?.value || "").trim();     const p1 = $("resetPass1")?.value || "";     const p2 = $("resetPass2")?.value || "";     if (!token) return setMsg("Missing reset token.");     if (!email) return setMsg("Missing email.");     if (p1.length < 6) return setMsg("Password too short (min 6).");     if (p1 !== p2) return setMsg("Passwords do not match.");     try {       await apiFetch("/api/auth/reset-password", {         method: "POST",         body: JSON.stringify({ email, token, password: p1 })       });       setMsg("Password updated. You can now sign in.");              history.replaceState(null, "", location.pathname);       const wrap = $("resetWrap"); if (wrap) wrap.style.display = "none";              const loginPassInput = $("authPassPage");       const loginPassLabel = loginPassInput ? loginPassInput.closest("label") : null;       if (loginPassLabel) loginPassLabel.style.display = "";       const toggle = $("authToggle");       if (toggle) toggle.style.display = "";      } catch (e) {       setMsg(e.message || "Could not reset password.");     }   }    const btnForgot = $("btnForgotPage");   if (btnForgot) btnForgot.addEventListener("click", requestPasswordReset);    const btnReset = $("btnResetPass");   if (btnReset) btnReset.addEventListener("click", doResetPassword);       const hp = getHashParams();   if (hp.reset) {     const wrap = $("resetWrap");     if (wrap) wrap.style.display = "block";     if (hp.email && $("authEmailPage")) $("authEmailPage").value = hp.email;   } })();  document.addEventListener("DOMContentLoaded", async () => {   if (typeof redirectToResetPageIfNeeded === "function" && redirectToResetPageIfNeeded()) return; const inReset = await maybeHandlePasswordReset(); if (inReset) return; }); 